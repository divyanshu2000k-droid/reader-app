# Design reference

34 artboards as plain HTML. Every value is inline in the markup.

Read `Components.dc.html` and `States.dc.html` first, they are the system.
`Journeys.dc.html` maps all eleven user journeys.

`docs/04-SCREENS.md` has a table mapping every screen to its file here.
Values come from `src/ui/theme.ts`. Read the HTML for layout and composition.
