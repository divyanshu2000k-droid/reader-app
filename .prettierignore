# Generated by drizzle-kit. Never hand-edited, so never hand-formatted.
src/db/migrations/

# THE ONE DELIBERATE EXCEPTION.
#
# theme.ts lays its type, space and radius scales out as aligned columns:
#
#   heading:     { size: 17, weight: '600', letterSpacing: -0.3, lineHeight: 22 }
#   bodyStrong:  { size: 14.5, weight: '600', letterSpacing: -0.15, lineHeight: 20 }
#
# Prettier collapses that to a single space after each colon. The alignment is the point:
# this file is a design system sheet expressed in code, and reading a scale as a table is
# how a wrong value gets spotted. Every other file in the repo is Prettier's.
src/ui/theme.ts

# Not ours.
android/
ios/
.expo/
dist/
node_modules/
